export function ShowcaseNote() {
  return (
    <section className="section note-section" id="showcase">
      <div className="container showcase-note">
        <span className="eyebrow">Native.Builder / NativelyAI Showcase</span>
        <h2>Packaging layer, not the coordination fabric</h2>
        <p>
          Native.Builder and NativelyAI frame the productized showcase for the
          hackathon. Band remains the collaboration layer where agents visibly
          coordinate. Provider APIs remain support paths for reasoning only
          when the workflow needs them.
        </p>
      </div>
    </section>
  );
}

