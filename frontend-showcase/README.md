# Workflow Legion Frontend Showcase

Static Vite + React + TypeScript showcase for Workflow Legion: Cyber Incident Command Room.

## Local setup

```bash
npm install
npm run build
```

For local viewing during development:

```bash
npm run dev
```

This showcase is fully static. It does not call a backend, does not use Supabase or auth, and does not require environment files or API keys.

